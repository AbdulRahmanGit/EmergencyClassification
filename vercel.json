{
    "builds": [{
        "src": "EmergencyClassification/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.12" },
        "installCommand": "apt-get update && apt-get install -y python3-distutils && pip install --upgrade setuptools && pip install --upgrade pip && pip install -r requirements.txt",
        "buildCommand": "pip install --upgrade pip && apt-get update && apt-get install -y python3-distutils && pip install --disable-pip-version-check --target . --upgrade -r requirements.txt"

    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "EmergencyClassification/wsgi.py"
        }
    ]
}
